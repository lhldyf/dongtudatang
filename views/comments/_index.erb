<% group = @group || @article.group %>
<div class="comments-template" id="article_comments_<%=@article.id%>">
  <div class="comments-wrap">
    <div class="comments">
          <% @offset ||= 0 %>
          <ol class="comment-list">
            <%= render :partial => @comments %>
          </ol>
    </div>
    <%= render :partial => 'comments/form' %>
  </div>
</div>
